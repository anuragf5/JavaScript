let angle1 = 60,
  angle2 = 60,
  angle3 = 61;

  let total = angle1+angle2+angle3;

  if(total == 180)
    {
        console.log(`Triangle is valid`);
    }
    else{
        console.log(`Triangle is not valid`);
    }
